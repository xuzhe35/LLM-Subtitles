# Dependency pinning strategy:
#   - Lower bound = oldest version known to work with this code path.
#   - Upper bound = next breaking major, to block surprise incompatibilities
#     (e.g. the OpenAI SDK had a major break at v1.0; transformers/torch ship
#     breaking releases roughly yearly).
#   - yt-dlp uses date-based versioning and must stay current to keep up with
#     YouTube site changes, so only a lower bound is set.
#
# When you bump an upper bound, smoke-test transcription + translation end to
# end before committing.

# --- Core runtime ---------------------------------------------------------
yt-dlp>=2024.8.0
openai>=1.40,<3.0
requests>=2.28,<3.0

# --- Optional: local Typhoon Whisper ASR runtime --------------------------
# Only needed if you select the "typhoon" transcription engine. The bounds
# below cover both transformers v4 and v5 (the Typhoon model is compatible
# with either).
torch>=1.13,<3.0
transformers>=4.40,<6.0
accelerate>=0.26,<2.0
huggingface_hub>=0.23,<2.0
safetensors>=0.4,<1.0
