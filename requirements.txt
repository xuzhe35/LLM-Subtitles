# Dependency pinning strategy:
#   - Lower bound = oldest version known to work with this code path.
#   - Upper bound = next breaking major, to block surprise incompatibilities
#     (e.g. the OpenAI SDK had a major break at v1.0).
#   - yt-dlp uses date-based versioning and must stay current to keep up with
#     YouTube site changes, so only a lower bound is set.
#
# When you bump an upper bound, smoke-test transcription + translation end to
# end before committing.

# --- Core runtime ---------------------------------------------------------
yt-dlp>=2024.8.0
openai>=1.40,<3.0
requests>=2.28,<3.0
websocket-client>=1.8,<2.0
certifi>=2024.7.4
