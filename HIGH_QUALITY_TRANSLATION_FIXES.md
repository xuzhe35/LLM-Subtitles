# 高质量翻译链路审查与修复记录

- 日期：2026-08-09
- 审查对象：当前工作区最新的 `transcribe_llm` 高质量翻译链路
- 状态：7 个已确认问题均已修复，并加入离线回归测试
- API Key：继续复用现有本地配置；本次未改写、打印或提交密钥
- 在线调用：本次验证未调用付费 OpenAI API，所有模型响应均由测试桩模拟

## 1. 审查范围

本次检查覆盖从视频元数据到最终字幕文件的完整高质量路径：

1. `youtube_subtitle_trans.py`：视频路由、音频缓存和产物命名。
2. `utils/semantic_transcriber.py`：语义转录、长音频分块和断点续传。
3. `utils/high_quality_pipeline.py`：时间转录、双分支编排、质量报告和最终组装。
4. `utils/transcript_aligner.py`：Unicode 分词、语义文本与可信时间轴对齐。
5. `utils/contextual_translator.py`：全局上下文、窗口翻译和结构校验。
6. `utils/subtitle_windows.py`：窗口规划、ID 覆盖及可信时间重建。
7. `utils/subtitle_formatter.py`：最终显示时间扩展和 SRT 输出。
8. 对应的单元测试、随机性质测试和完整流水线模拟测试。

## 2. 修复摘要

| 编号 | 严重度 | 问题 | 修复后的行为 |
|---|---|---|---|
| HQ-01 | 严重 | 同名视频共用标题缓存，可能复用另一个视频的音频和检查点 | 所有真实视频产物使用“安全标题 + 视频 ID”；缺少 ID 时使用 URL 的稳定摘要 |
| HQ-02 | 严重 | 重叠 ASR 片段被强制推后，并伪造 `0.01s` 时间片 | 原样保留可信重叠；无效时间直接报错；合并 cue 使用全部来源时间的并集 |
| HQ-03 | 高 | 模型可用空字符串静默删除真实语音，且分组删除统计失真 | 高质量翻译禁止空文本；失败窗口重试/升级，仍为空则明确失败；分别统计来源 cue 和输出 cue |
| HQ-04 | 高 | 同一 fallback 文本出现 3 次就被自动删除 | 所有 fallback 都保留；重复、异语种短词和上游怀疑项只写入风险标记和质量报告 |
| HQ-05 | 中 | 大文件时间分支按固定点硬切，可能在句中切断 | 两个 ASR 分支共享静音边界；时间分支增加 4 秒前置重叠并过滤重叠区的前置副本 |
| HQ-06 | 中 | 阿拉伯、斯拉夫、天城文、柬埔寨文等脚本得到零 token | 改为 Unicode 类别驱动分词，并区分有空格与无空格书写系统 |
| HQ-07 | 低 | 最后一条长字幕可被延长到媒体结束时间之后 | 最后一条只能延长到真实媒体时长；时长未知时不再猜测性延长 |

## 3. 详细修复说明

### HQ-01：同名视频缓存串用

#### 原因

音频缓存路径原来只包含 `video.safe_title`。两个不同视频只要标题相同，就会命中同一个 `*_audio.*` 文件。高质量流水线又以同一个标题生成语义转录、时间转录、对齐和翻译检查点，因此错误音频会继续污染整个下游产物链。

#### 修复

- `VideoSource` 新增可选 `video_id` 和只读 `artifact_stem`。
- 下载器提供 ID 时，真实产物名使用 `<safe-title>.<safe-video-id>`。
- 元数据没有 ID 时，使用原始 URL 的 SHA-256 前 12 位作为稳定隔离键。
- 超长 ID 会保留可读前缀并追加摘要；标题部分按剩余长度裁剪，完整 stem 不超过原有文件名预算。
- 音频、增强音频、手工字幕、Realtime 检查点、最终 SRT 以及高质量全部阶段产物统一使用该 stem。
- 手工构造且没有 `video_id` 的 `VideoSource` 保持旧行为，避免破坏内部兼容调用。

#### 迁移影响

真实视频第一次使用新版本时不会复用旧的“纯标题”缓存，会重新下载/生成一次。这是有意的安全失效：旧缓存无法证明属于当前视频。之后相同视频 ID 会继续正常复用新检查点。

### HQ-02：重叠时间被移位并伪造

#### 原因

对齐器原来为了强制 cue 不重叠，会把后一个 cue 的 `start` 推到前一个 cue 的 `end`；若 `end <= start`，再人为设置 `end = start + 0.01`。例如可信证据 `alpha=0-10s`、`beta=5-6s` 会把 `beta` 改成 `10-10.01s`，完全离开它的证据区间。

#### 修复

- 删除所有强制非重叠和 `0.01s` 补丁逻辑。
- cue 仍按开始时间稳定排序，但合法重叠保持原样；SRT 允许时间重叠。
- 对非有限数、负开始时间和非正时长直接抛出错误，停止高质量链路，而不是猜测时间。
- 当翻译模型合并相邻 ID 时，时间由所有来源 cue 的 `min(start)` 和 `max(end)` 重建，不再假设“最后一个 cue 的 end 一定最大”。
- 翻译窗口的持续时间限制同样改为计算全部证据的时间并集。

#### 新的不变量

任何输出 cue 的时间必须来自它引用的时间证据；程序可以保留重叠，也可以合并证据并集，但不能移动、插值或发明时间。

### HQ-03：空翻译可静默删掉真实语音

#### 原因

高质量翻译曾启用 `allow_empty_text=True`。模型只要返回空字符串，该项就会被标记为已覆盖但不渲染。空项还绕过合并时长和跨说话人限制。最终覆盖率仍可显示 100%，而 `dropped_count` 只统计输出组数，不统计一个空组吞掉了多少来源 cue。

#### 修复

- 翻译提示明确要求每个输出 cue 都包含非空译文。
- `validate_translation_window` 禁止空文本，空文本成为结构错误。
- 主模型会按现有重试策略重试；允许选择性升级时可交给升级模型；持续为空则明确失败并保留断点，不生成“看似完整”的字幕。
- 最终组装增加防御性检查，即使旧检查点或未来改动绕过前置校验，也会立即停止。
- 质量报告区分：
  - `translated_cue_count` / `dropped_cue_count`：来源 cue 数量；
  - `translated_output_cue_count` / `dropped_output_cue_count`：合并后的输出组数量。
- 新链路正常情况下两个 dropped 计数都必须为 0。

### HQ-04：重复 fallback 被误删

#### 原因

未被语义文本覆盖的时间片段如果规范化文本出现至少 3 次，会直接从候选集合删除。片头、片尾或副歌中真实重复的“thank you”等内容会因此全部消失。短暂的异语种词和上游 `suspect_hallucination` 也存在相同的不可逆删除风险。

#### 修复

- 删除三个自动删除分支。
- 风险判断继续保留，但只产生：
  - cue `flags`：`repeated_fallback_loop`、`isolated_foreign_word` 或 `suspect_hallucination`；
  - 对齐产物 `fallback_warnings`；
  - 质量报告 `alignment.fallback_warnings`。
- `dropped_fallbacks` 为兼容旧消费者继续存在，但新版本固定为空；`dropped_fallback_cues` 固定为 0。
- 如果风险片段后来承载了未解析语义文本的 rescue cue，风险标记会继续随 cue 保留。

该策略优先保证“不丢真实语音”。是否需要人工去除水印或噪声，应由可审计的后处理完成，不能由弱启发式静默决定。

### HQ-05：长音频在句中硬切

#### 原因

超过 24 MB 时，时间分支原来调用分块规划器但设置 `overlap_sec=0.0`，同时没有提供自然边界。固定切点可能落在单词或句子中间，导致切点附近丢词、重复或错误时间。

OpenAI 官方文件转录文档说明单文件上限为 25 MB，并建议大文件分块时避免在句子中间切分，以免丢失上下文、降低准确率：[File transcription — Longer inputs](https://developers.openai.com/api/docs/guides/speech-to-text#longer-inputs)。工程继续使用 24 MB 作为安全余量。

#### 修复

- `audio_splitter.find_silence_boundaries` 使用 FFmpeg `silencedetect` 找出静音段中点。
- 大文件开始双分支之前只检测一次，并把同一组边界传给语义和时间分支。
- 分块规划器在目标点附近优先吸附到静音边界。
- 时间分支加入 4 秒前置重叠，为切点后的识别提供前文。
- 合并时间结果时，后一个块仅拥有上一个非重叠切点之后的片段；片段中点位于切点之前的前置副本会被过滤。
- 检查点记录 `overlap_segments_discarded`，便于排查重叠过滤量。
- FFmpeg 检测不可用或找不到静音时不阻塞任务，退回到“固定切点 + 4 秒重叠”的安全降级路径。

### HQ-06：Unicode 脚本退化为零 token

#### 原因

旧正则只覆盖拉丁字母、数字、泰文和部分 CJK。阿拉伯文、西里尔文、天城文和柬埔寨文等会得到零个 canonical token，对齐器随后静默退化为时间文本 fallback，但质量报告不一定标明语义对齐已经失效。

#### 修复

- 分词器改用 `unicodedata.category` 识别 Unicode 字母、数字和组合标记。
- 通常使用空格的脚本按词分组，例如拉丁、阿拉伯、西里尔、天城、希伯来和希腊。
- CJK、泰、老挝、缅甸、柬埔寨等通常无空格的脚本按基础字符加组合标记形成 grapheme-like token。
- 数字仍保留小数点、逗号和时间冒号形式。
- token 回拼会在有空格脚本之间恢复空格，在无空格脚本之间直接连接。
- 句末识别新增阿拉伯问号、乌尔都句号和天城文 danda。

回归样本已覆盖阿拉伯文、俄文、印地文和柬埔寨文，确认它们走 canonical 对齐且获得完整置信度，而不是零 token fallback。

### HQ-07：末条字幕超过媒体结束时间

#### 原因

显示时间扩展对最后一个 cue 使用 `start + reading_time` 作为上限。若最后的可信语音为 `99-100s`，长文本可把字幕延长到 `111s`，超过实际媒体结尾。

#### 修复

- `extend_display_times` 新增 `media_duration` 参数。
- 高质量流水线从音频读取真实时长并传入格式化阶段。
- 读取失败时，以时间转录中最大的可信 `end` 作为保守上限。
- 最后一条可使用媒体剩余尾部，但绝不因阅读时长扩展而超过上限。
- 独立调用者没有提供媒体时长时，最后一条保持证据 `end`，不做猜测性扩展。

## 4. 版本与检查点失效

为防止旧行为的检查点绕过修复，本次更新了以下身份版本：

- 高质量流水线：`transcribe_llm_v1` → `transcribe_llm_v2`
- 翻译流水线：`transcribe_llm_translate_v1` → `transcribe_llm_translate_v2`
- 翻译提示：`translate-2026-08-08.1` → `translate-2026-08-09.2`
- 对齐器：`aligner-2026-08-09.1` → `aligner-2026-08-09.2`

实际效果：旧高质量检查点不会被当成新版本结果复用；新版本完成一次后仍会继续按音频哈希、模型、语言、分块计划和目标语言正常复用。

## 5. 回归测试

新增或更新的关键测试包括：

1. 同标题、不同视频 ID 会产生不同音频缓存路径。
2. `0-10s` 与 `5-6s` 的重叠证据保持不变，不生成 `10-10.01s`。
3. 非正时间证据会失败，不会被自动修补。
4. 重复 fallback、异语种短词和 suspect 标记都保留文本并写入 warning。
5. 翻译返回空字符串会触发结构重试；持续为空时整条流水线失败。
6. 重叠 cue 合并后覆盖所有来源证据的最小开始和最大结束。
7. 时间分块包含 4 秒重叠，并可吸附到提供的自然边界。
8. FFmpeg 静音输出可解析为静音中点；FFmpeg 不可用时安全返回空边界。
9. 阿拉伯文、俄文、印地文、柬埔寨文均能完成 canonical 对齐。
10. 最后一条字幕在未知媒体时长时不外延，在已知时长时严格封顶。
11. 200 个随机对齐场景继续验证：文本不重复消费、cue 开始时间有序、合法重叠保留、显示扩展不制造新重叠。

本地验证结果：

```text
.venv/bin/python -m unittest discover -s tests
Ran 215 tests in 4.287s
OK

.venv/bin/python -m compileall -q youtube_subtitle_trans.py main.py utils tests tools
compileall: OK
```

## 6. 官方接口核对

本次同时根据官方文档核对了当前使用的模型和转录接口形状：

- [`gpt-transcribe` 模型](https://developers.openai.com/api/docs/models/gpt-transcribe)
- [文件转录、文件大小、时间戳和长输入说明](https://developers.openai.com/api/docs/guides/speech-to-text)
- [`gpt-5.6-terra` 模型](https://developers.openai.com/api/docs/models/gpt-5.6-terra)

当前 Python 虚拟环境的 OpenAI SDK 为 `2.45.0`。现有 `gpt-transcribe`、`whisper-1` 的 `verbose_json`/word timestamps，以及 `gpt-4o-transcribe-diarize` 的 `diarized_json`/`chunking_strategy="auto"` 请求形状未发现明显不兼容。

## 7. 未执行与剩余风险

### 未执行

- 未发起真实 OpenAI API 请求，因此没有消耗额度，也没有验证特定账户的模型权限、限流和网络状态。
- 未对一部长于 25 MB 的真实多人长视频做人耳逐句对比；当前验证覆盖代码路径、边界规划和模拟响应。

### 剩余风险

1. 静音检测是启发式：持续音乐、无静音演讲或高噪声录音可能找不到自然边界，此时会使用重叠降级方案。
2. 不同播放器对合法重叠 SRT cue 的视觉叠放方式可能不同。当前选择以“不得篡改可信时间”为最高优先级。
3. 疑似水印/噪声现在会保留而不是自动删除，字幕可能多出需要人工确认的内容；质量报告会提供明确 warning 位置。
4. 首次运行新版本会因视频 ID 命名和版本升级重新生成旧缓存。这是修复跨视频污染与旧检查点兼容风险所必需的成本。

## 8. 建议的真实媒体验收

在准备正式大量处理前，建议选一段包含以下特征的公开视频做一次人工验收：

- 文件大于 25 MB，切点附近有连续说话；
- 至少两名说话人并存在抢话或时间重叠；
- 包含真实重复句、副歌或片尾感谢语；
- 包含阿拉伯文/俄文/印地文/柬埔寨文中的一种；
- 最后一条语音接近媒体结束。

验收时重点查看 `*.aligned.json` 的原始时间、`fallback_warnings`、`*.quality.json` 的结构门禁，以及最终 SRT 的最后结束时间。真实调用属于付费操作，应在明确需要时单独执行。
